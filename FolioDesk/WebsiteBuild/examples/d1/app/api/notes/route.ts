// Unused Cloudflare D1 example route, not part of the live FolioDesk app
// (this sits outside the real app/ router, so Next.js never actually served it).
// Emptied to avoid pulling in Cloudflare-only modules during `next build`.
export {};
