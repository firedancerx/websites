import {NextResponse} from "next/server";import {db} from "../../../lib/db";import {createSession,verifyPassword,getBaseUrl} from "../../../lib/auth";
export async function POST(req:Request){const f=await req.formData(),email=String(f.get("email")||"").toLowerCase(),password=String(f.get("password")||"");try{const [rows]=await db().execute<DatabaseRow[]>("SELECT id,password_hash,role,status FROM users WHERE email=? LIMIT 1",[email]);const u=rows[0];if(!u||u.status!=="ACTIVE"||!verifyPassword(password,u.password_hash))throw new Error();await createSession(u.id);return NextResponse.redirect(new URL(u.role==="ADMIN"?"/foliodesk/admin":"/foliodesk/portal",getBaseUrl(req)),303)}catch{return NextResponse.redirect(new URL("/foliodesk/login?error=Invalid+email+or+password",getBaseUrl(req)),303)}}
