// Unused Cloudflare D1/Drizzle starter-template config.
// The live FolioDesk app uses lib/db.ts (mysql2) and scripts/migrate.mjs for its database layer instead.
// Emptied because "drizzle-kit" isn't a project dependency, which breaks `next build`'s type-check.
export {};
